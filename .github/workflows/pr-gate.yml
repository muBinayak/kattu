name: PR Gate

on:
  workflow_call:
    inputs:
      SERVICE_LOCATION:
        required: false
        type: string
        default: '.'
        description: 'Base path used by all stacks unless a type-specific override is set'
      JAVA_SERVICE_LOCATION:
        required: false
        type: string
        default: ''
        description: 'Java service root; falls back to SERVICE_LOCATION if empty'
      GO_SERVICE_LOCATION:
        required: false
        type: string
        default: ''
        description: 'Go service root; falls back to SERVICE_LOCATION if empty'
      NPM_SERVICE_LOCATION:
        required: false
        type: string
        default: ''
        description: 'npm service root; falls back to SERVICE_LOCATION if empty'
      ANDROID_SERVICE_LOCATION:
        required: false
        type: string
        default: ''
        description: 'Android project root; falls back to SERVICE_LOCATION if empty'
      FLUTTER_SERVICE_LOCATION:
        required: false
        type: string
        default: ''
        description: 'Flutter project root; falls back to SERVICE_LOCATION if empty'
      JAVA_VERSION:
        required: false
        type: number
        default: 21
      NODE_VERSION:
        required: false
        type: string
        default: '20'
      FLUTTER_VERSION:
        required: false
        type: string
        default: ''
        description: 'Flutter SDK version; empty = latest of the stable channel'
      SCAN_SEVERITY:
        required: false
        type: string
        default: 'HIGH,CRITICAL'
      trivy_comment_mode:
        description: '"full" = comment all current HIGH/CRITICAL; "diff" = comment only newly introduced ones'
        required: false
        type: string
        default: 'full'
      NPM_COVERAGE_COMMAND:
        description: 'Command to run tests and generate coverage. Override for non-Jest runners (e.g. "npx vitest run --coverage" for Vitest).'
        required: false
        type: string
        default: 'npm test -- --coverage'
    secrets:
      OSSRH_USER:
        required: false
      OSSRH_SECRET:
        required: false
      CODECOV_LICENSE:
        required: false
      GITLEAKS_LICENSE:
        required: false

jobs:
# ─────────────────────────────────────────────
# DETECT — Check for stack marker files at the
# declared service locations (not a repo-wide
# search). Security jobs run in parallel with
# this; coverage jobs wait for its output.
# ─────────────────────────────────────────────
  detect:
    name: Detect Stacks
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      has_java:         ${{ steps.detect.outputs.has_java }}
      has_go:           ${{ steps.detect.outputs.has_go }}
      has_npm:          ${{ steps.detect.outputs.has_npm }}
      has_android:      ${{ steps.detect.outputs.has_android }}
      has_flutter:      ${{ steps.detect.outputs.has_flutter }}
      java_location:    ${{ steps.detect.outputs.java_location }}
      go_location:      ${{ steps.detect.outputs.go_location }}
      npm_location:     ${{ steps.detect.outputs.npm_location }}
      android_location: ${{ steps.detect.outputs.android_location }}
      flutter_location: ${{ steps.detect.outputs.flutter_location }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - name: Detect stacks
        id: detect
        run: |
          # Resolve each stack location: use the type-specific input when set,
          # otherwise fall back to SERVICE_LOCATION. Single-stack callers only
          # need SERVICE_LOCATION; multi-stack callers set per-type overrides.
          fexists() { [ -f "$1" ] && echo "true" || echo "false"; }
          BASE="${{ inputs.SERVICE_LOCATION }}"
          resolve() { local v="$1"; [ -n "$v" ] && echo "$v" || echo "$BASE"; }

          JAVA_LOC="$(resolve "${{ inputs.JAVA_SERVICE_LOCATION }}")"
          GO_LOC="$(resolve "${{ inputs.GO_SERVICE_LOCATION }}")"
          NPM_LOC="$(resolve "${{ inputs.NPM_SERVICE_LOCATION }}")"
          ANDROID_LOC="$(resolve "${{ inputs.ANDROID_SERVICE_LOCATION }}")"
          FLUTTER_LOC="$(resolve "${{ inputs.FLUTTER_SERVICE_LOCATION }}")"

          echo "has_java=$(fexists "$JAVA_LOC/pom.xml")"       >> $GITHUB_OUTPUT
          echo "has_go=$(fexists "$GO_LOC/go.mod")"             >> $GITHUB_OUTPUT
          echo "has_npm=$(fexists "$NPM_LOC/package.json")"     >> $GITHUB_OUTPUT
          if [ -f "$ANDROID_LOC/settings.gradle" ] || [ -f "$ANDROID_LOC/settings.gradle.kts" ]; then
            echo "has_android=true"  >> $GITHUB_OUTPUT
          else
            echo "has_android=false" >> $GITHUB_OUTPUT
          fi
          echo "has_flutter=$(fexists "$FLUTTER_LOC/pubspec.yaml")" >> $GITHUB_OUTPUT

          echo "java_location=$JAVA_LOC"       >> $GITHUB_OUTPUT
          echo "go_location=$GO_LOC"           >> $GITHUB_OUTPUT
          echo "npm_location=$NPM_LOC"         >> $GITHUB_OUTPUT
          echo "android_location=$ANDROID_LOC" >> $GITHUB_OUTPUT
          echo "flutter_location=$FLUTTER_LOC" >> $GITHUB_OUTPUT

      - name: Print detected stacks
        run: |
          echo "Java:    ${{ steps.detect.outputs.has_java }}  (${{ steps.detect.outputs.java_location }})"
          echo "Go:      ${{ steps.detect.outputs.has_go }}  (${{ steps.detect.outputs.go_location }})"
          echo "npm:     ${{ steps.detect.outputs.has_npm }}  (${{ steps.detect.outputs.npm_location }})"
          echo "Android: ${{ steps.detect.outputs.has_android }}  (${{ steps.detect.outputs.android_location }})"
          echo "Flutter: ${{ steps.detect.outputs.has_flutter }}  (${{ steps.detect.outputs.flutter_location }})"

# ─────────────────────────────────────────────
# TIER 1 — Security (always run, all parallel)
# ─────────────────────────────────────────────
  secret-scan:
    name: Secret Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411  # v2.19.4
        with:
          egress-policy: audit

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
        with:
          fetch-depth: 0

      - uses: gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e  # v3.0.0
        env:
          GITHUB_TOKEN:     ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}

  dependency-review:
    name: Dependency CVE Review
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411  # v2.19.4
        with:
          egress-policy: audit

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294  # v5.0.0
        with:
          fail-on-severity: high
          comment-summary-in-pr: always

  lint-workflows:
    name: Lint GitHub Actions Workflows
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411  # v2.19.4
        with:
          egress-policy: audit

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - name: Install actionlint
        run: |
          ACTIONLINT_VERSION="1.7.7"
          curl -sSL "https://github.com/rhysd/actionlint/releases/download/v${ACTIONLINT_VERSION}/actionlint_${ACTIONLINT_VERSION}_linux_amd64.tar.gz" \
            | tar xz actionlint
          sudo mv actionlint /usr/local/bin/

      - name: Run actionlint
        run: actionlint

  trivy-fs:
    name: Trivy Filesystem Vulnerability Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      pull-requests: write
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411  # v2.19.4
        with:
          egress-policy: audit

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      # ── Step A: Table scan for comment (never fails the job) ──────────────
      - name: Trivy scan (table report for comment)
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          severity: ${{ inputs.SCAN_SEVERITY }}
          exit-code: '0'
          format: table
          output: trivy-report.txt

      # ── Step B (diff mode): Checkout base ref for comparison ──────────────
      - name: Checkout base ref for diff comparison
        if: inputs.trivy_comment_mode == 'diff' && github.event_name == 'pull_request'
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          path: trivy-base

      - name: Trivy scan base ref (JSON for diff)
        if: inputs.trivy_comment_mode == 'diff' && github.event_name == 'pull_request'
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0
        with:
          scan-type: fs
          scan-ref: trivy-base
          severity: ${{ inputs.SCAN_SEVERITY }}
          exit-code: '0'
          format: json
          output: trivy-base.json

      - name: Trivy scan head ref (JSON for diff)
        if: inputs.trivy_comment_mode == 'diff' && github.event_name == 'pull_request'
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          severity: ${{ inputs.SCAN_SEVERITY }}
          exit-code: '0'
          format: json
          output: trivy-head.json

      # ── Step C: Build Markdown comment body ──────────────────────────────
      - name: Build Trivy comment body
        run: |
          COMMENT_FILE="trivy-comment.md"
          MODE="${{ inputs.trivy_comment_mode }}"

          if [ "$MODE" = "diff" ] && [ -f "trivy-base.json" ] && [ -f "trivy-head.json" ]; then
            base_ids=$(jq -r '.Results[]?.Vulnerabilities[]? | .PkgName + ":::" + .VulnerabilityID' trivy-base.json 2>/dev/null | sort -u || true)
            new_rows=""
            while IFS=$'\t' read -r pkg vuln_id severity title; do
              if ! printf '%s\n' "$base_ids" | grep -qF "${pkg}:::${vuln_id}"; then
                new_rows="${new_rows}| ${pkg} | ${vuln_id} | ${severity} | ${title} |\n"
              fi
            done < <(jq -r '.Results[]?.Vulnerabilities[]? | [.PkgName, .VulnerabilityID, .Severity, (.Title // "N/A")] | @tsv' trivy-head.json 2>/dev/null || true)

            if [ -n "$new_rows" ]; then
              {
                echo "## Trivy — New HIGH/CRITICAL Findings"
                echo ""
                echo "These vulnerabilities were **introduced by this PR**:"
                echo ""
                echo "| Package | CVE | Severity | Title |"
                echo "|---|---|---|---|"
                printf '%b' "$new_rows"
              } > "$COMMENT_FILE"
            else
              {
                echo "## Trivy Security Scan"
                echo "✅ No NEW HIGH/CRITICAL vulnerabilities introduced by this PR."
              } > "$COMMENT_FILE"
            fi
          else
            if grep -qE "^Total: [1-9]" trivy-report.txt 2>/dev/null; then
              {
                echo "## Trivy Security Scan Findings"
                echo ""
                echo '```'
                cat trivy-report.txt
                echo '```'
              } > "$COMMENT_FILE"
            else
              {
                echo "## Trivy Security Scan"
                echo "✅ No HIGH/CRITICAL findings."
              } > "$COMMENT_FILE"
            fi
          fi

      # ── Step D: Post sticky PR comment (skip on forks — token is read-only) ─
      - name: Post Trivy findings comment
        # Comment only on same-repo PRs (the default token can't write to a base repo from a
        # cross-fork PR). NOTE: head.repo.fork is true whenever the repo itself is a fork of an
        # upstream, so compare head/base repo names instead of using !head.repo.fork.
        if: github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.event.pull_request.base.repo.full_name
        uses: marocchino/sticky-pull-request-comment@0ea0beb66eb9baf113663a64ec522f60e49231c0  # v3.0.4
        with:
          header: trivy
          path: trivy-comment.md

      # ── SARIF upload for Security tab (always, independent of gate) ───────
      - name: Trivy SARIF scan
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          severity: ${{ inputs.SCAN_SEVERITY }}
          exit-code: '0'
          format: sarif
          output: trivy-fs.sarif

      - name: Upload SARIF to Security tab
        uses: github/codeql-action/upload-sarif@4e828ff8d448a8a6e532957b1811f387a63867e8  # v3.29.4
        if: always()
        with:
          sarif_file: trivy-fs.sarif
          category: trivy-fs

      # ── Step E: Gate — fail the job if HIGH/CRITICAL exist ────────────────
      - name: Gate on HIGH/CRITICAL findings
        run: |
          if grep -qE "^Total: [1-9]" trivy-report.txt 2>/dev/null; then
            echo "::error::HIGH or CRITICAL vulnerabilities found. Review the Trivy comment on this PR."
            exit 1
          fi
          echo "No HIGH or CRITICAL findings."

  trivy-config:
    name: Trivy Dockerfile / IaC Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411  # v2.19.4
        with:
          egress-policy: audit

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - name: Scan Dockerfiles and IaC configs
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0
        with:
          scan-type: config
          scan-ref: .
          severity: ${{ inputs.SCAN_SEVERITY }}
          exit-code: 0
          format: sarif
          output: trivy-config.sarif

      - name: Upload results to Security tab
        uses: github/codeql-action/upload-sarif@4e828ff8d448a8a6e532957b1811f387a63867e8  # v3.29.4
        if: always()
        with:
          sarif_file: trivy-config.sarif
          category: trivy-config

  semgrep:
    name: Semgrep SAST
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411  # v2.19.4
        with:
          egress-policy: audit

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      # Polyglot SAST across every stack in one job (Go, Java/Kotlin, JS/TS, etc.)
      # using Semgrep's free, public OSS rulesets — the one tool here that covers
      # all detected stacks without a per-language matrix or build step. Pinned to
      # an exact version via pip (no first-party pinnable action exists), matching
      # the actionlint install pattern below. Advisory only: semgrep exits 0 on
      # findings unless --error is passed, so this never fails the PR — findings
      # surface via the SARIF upload to the Security tab instead.
      - name: Install Semgrep
        run: pip install "semgrep==1.177.0"

      - name: Run Semgrep
        run: |
          semgrep scan \
            --config p/security-audit \
            --config p/secrets \
            --config p/owasp-top-ten \
            --sarif --output semgrep.sarif \
            .

      - name: Upload SARIF to Security tab
        uses: github/codeql-action/upload-sarif@4e828ff8d448a8a6e532957b1811f387a63867e8  # v3.29.4
        if: always()
        with:
          sarif_file: semgrep.sarif
          category: semgrep

# ─────────────────────────────────────────────
# CodeQL — one job per detected language, gated on detect's output. Unlike the
# codeql.yml files already present elsewhere in the MOSIP/Inji ecosystem, the
# compiled languages (Go, Java/Kotlin) use build-mode: autobuild instead of
# build-mode: none — "none" skips compilation entirely, which for a compiled
# language means CodeQL cannot fully resolve types/dataflow and yields a much
# shallower scan. build-mode: none is correct only for JS/TS, which is not a
# compiled language and needs no build step for full-fidelity analysis.
# CodeQL's own analyze step does not fail the job on findings — alerts surface
# via the Security tab, so these jobs are advisory by default, no extra
# handling needed.
# ─────────────────────────────────────────────
  codeql-go:
    name: CodeQL (Go)
    needs: detect
    if: ${{ needs.detect.outputs.has_go == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411  # v2.19.4
        with:
          egress-policy: audit

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16  # v6.5.0
        with:
          go-version-file: ${{ needs.detect.outputs.go_location }}/go.mod
          cache: true

      - name: Initialize CodeQL
        uses: github/codeql-action/init@4e828ff8d448a8a6e532957b1811f387a63867e8  # v3.29.4
        with:
          languages: go
          build-mode: autobuild

      - name: Autobuild
        uses: github/codeql-action/autobuild@4e828ff8d448a8a6e532957b1811f387a63867e8  # v3.29.4

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@4e828ff8d448a8a6e532957b1811f387a63867e8  # v3.29.4
        with:
          category: "/language:go"

  codeql-javascript:
    name: CodeQL (JavaScript/TypeScript)
    needs: detect
    if: ${{ needs.detect.outputs.has_npm == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411  # v2.19.4
        with:
          egress-policy: audit

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - name: Initialize CodeQL
        uses: github/codeql-action/init@4e828ff8d448a8a6e532957b1811f387a63867e8  # v3.29.4
        with:
          languages: javascript-typescript
          build-mode: none

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@4e828ff8d448a8a6e532957b1811f387a63867e8  # v3.29.4
        with:
          category: "/language:javascript-typescript"

  codeql-java:
    name: CodeQL (Java/Kotlin)
    needs: detect
    if: ${{ needs.detect.outputs.has_java == 'true' || needs.detect.outputs.has_android == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411  # v2.19.4
        with:
          egress-policy: audit

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - uses: actions/setup-java@ad2b38190b15e4d6bdf0c97fb4fca8412226d287  # v5.3.0
        with:
          distribution: temurin
          java-version: ${{ inputs.JAVA_VERSION }}

      - name: Initialize CodeQL
        uses: github/codeql-action/init@4e828ff8d448a8a6e532957b1811f387a63867e8  # v3.29.4
        with:
          languages: java-kotlin
          build-mode: autobuild

      - name: Autobuild
        uses: github/codeql-action/autobuild@4e828ff8d448a8a6e532957b1811f387a63867e8  # v3.29.4

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@4e828ff8d448a8a6e532957b1811f387a63867e8  # v3.29.4
        with:
          category: "/language:java-kotlin"


# ─────────────────────────────────────────────
# TIER 2 — Coverage (conditional on detect output, all parallel)
# ─────────────────────────────────────────────
  coverage-java:
    name: Java Coverage → Codecov
    needs: detect
    if: ${{ needs.detect.outputs.has_java == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411  # v2.19.4
        with:
          egress-policy: audit

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - uses: actions/setup-java@ad2b38190b15e4d6bdf0c97fb4fca8412226d287  # v5.3.0
        with:
          distribution: temurin
          java-version: ${{ inputs.JAVA_VERSION }}

      - name: Cache Maven repository
        uses: actions/cache@2c8a9bd7457de244a408f35966fab2fb45fda9c8  # v6.0.0
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      - name: Create Maven settings
        run: |
          cat > $GITHUB_WORKSPACE/settings.xml <<'EOF'
          <settings>
            <servers>
              <server>
                <id>ossrh</id>
                <username>${{ secrets.OSSRH_USER }}</username>
                <password>${{ secrets.OSSRH_SECRET }}</password>
              </server>
            </servers>
          </settings>
          EOF

      - name: Run tests and generate coverage
        run: |
          cd ${{ needs.detect.outputs.java_location }}
          mvn -U -B verify -Dgpg.skip=true -Dmaven.wagon.http.retryHandler.count=2 \
            --file pom.xml -s $GITHUB_WORKSPACE/settings.xml

      - name: Upload to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f  # v7.0.0
        with:
          token: ${{ secrets.CODECOV_LICENSE }}
          files: '${{ needs.detect.outputs.java_location }}/**/target/site/jacoco/jacoco.xml'
          flags: java
          fail_ci_if_error: true

  coverage-go:
    name: Go Coverage → Codecov
    needs: detect
    if: ${{ needs.detect.outputs.has_go == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411  # v2.19.4
        with:
          egress-policy: audit

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16  # v6.5.0
        with:
          go-version-file: ${{ needs.detect.outputs.go_location }}/go.mod
          cache: true

      - name: Run tests and generate coverage
        run: |
          cd ${{ needs.detect.outputs.go_location }}
          go test -v -coverprofile=coverage.out -covermode=atomic ./...

      - name: Upload to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f  # v7.0.0
        with:
          token: ${{ secrets.CODECOV_LICENSE }}
          files: ${{ needs.detect.outputs.go_location }}/coverage.out
          flags: go
          fail_ci_if_error: true

  coverage-npm:
    name: npm Coverage → Codecov
    needs: detect
    if: ${{ needs.detect.outputs.has_npm == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411  # v2.19.4
        with:
          egress-policy: audit

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0
        with:
          node-version: ${{ inputs.NODE_VERSION }}
          cache: npm
          cache-dependency-path: '${{ needs.detect.outputs.npm_location }}/package-lock.json'

      - name: Install dependencies
        run: |
          cd ${{ needs.detect.outputs.npm_location }}
          npm ci

      - name: Run tests and generate coverage
        run: |
          cd ${{ needs.detect.outputs.npm_location }}
          ${{ inputs.NPM_COVERAGE_COMMAND }}

      - name: Upload to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f  # v7.0.0
        with:
          token: ${{ secrets.CODECOV_LICENSE }}
          files: '${{ needs.detect.outputs.npm_location }}/coverage/lcov.info'
          flags: npm
          fail_ci_if_error: true

  coverage-android:
    name: Android Coverage → Codecov
    needs: detect
    if: ${{ needs.detect.outputs.has_android == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411  # v2.19.4
        with:
          egress-policy: audit

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - uses: actions/setup-java@ad2b38190b15e4d6bdf0c97fb4fca8412226d287  # v5.3.0
        with:
          distribution: temurin
          java-version: ${{ inputs.JAVA_VERSION }}

      - name: Run Android unit tests with coverage
        run: |
          cd ${{ needs.detect.outputs.android_location }}
          ./gradlew testDebugUnitTest jacocoTestReport

      - name: Upload to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f  # v7.0.0
        with:
          token: ${{ secrets.CODECOV_LICENSE }}
          files: '${{ needs.detect.outputs.android_location }}/**/build/reports/jacoco/**/*.xml'
          flags: android
          fail_ci_if_error: true

  coverage-flutter:
    name: Flutter Coverage → Codecov
    needs: detect
    if: ${{ needs.detect.outputs.has_flutter == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411  # v2.19.4
        with:
          egress-policy: audit

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2  # v2.23.0
        with:
          channel: stable
          flutter-version: ${{ inputs.FLUTTER_VERSION }}
          cache: true

      - name: Install dependencies
        run: |
          cd ${{ needs.detect.outputs.flutter_location }}
          flutter pub get

      - name: Run tests and generate coverage
        run: |
          cd ${{ needs.detect.outputs.flutter_location }}
          flutter test --coverage

      - name: Upload to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f  # v7.0.0
        with:
          token: ${{ secrets.CODECOV_LICENSE }}
          files: '${{ needs.detect.outputs.flutter_location }}/coverage/lcov.info'
          flags: flutter
          fail_ci_if_error: true

# ─────────────────────────────────────────────
# TIER 2 — Supply-chain integrity (POM-gated)
# Verify PGP signatures of Maven dependencies via
# simplify4u/pgpverify-maven-plugin. Advisory only:
# never fails the PR, but posts a sticky comment
# flagging unsigned / invalid / untrusted deps.
# ─────────────────────────────────────────────
  verify-maven-signatures:
    name: Maven Dependency Signature Check (pgpverify)
    needs: detect
    if: ${{ needs.detect.outputs.has_java == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411  # v2.19.4
        with:
          egress-policy: audit

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - uses: actions/setup-java@ad2b38190b15e4d6bdf0c97fb4fca8412226d287  # v5.3.0
        with:
          distribution: temurin
          java-version: ${{ inputs.JAVA_VERSION }}

      - name: Cache Maven repository
        uses: actions/cache@2c8a9bd7457de244a408f35966fab2fb45fda9c8  # v6.0.0
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-pgpverify-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      - name: Create Maven settings
        run: |
          cat > $GITHUB_WORKSPACE/settings.xml <<'EOF'
          <settings>
            <servers>
              <server>
                <id>ossrh</id>
                <username>${{ secrets.OSSRH_USER }}</username>
                <password>${{ secrets.OSSRH_SECRET }}</password>
              </server>
            </servers>
          </settings>
          EOF

      # pgpverify:check verifies PGP signatures of all resolved dependencies.
      # Plugin version is pinned (supply-chain: never LATEST/RELEASE). The result
      # is captured but never propagated — this gate is advisory, so we force the
      # step green and let the comment step surface any problems.
      - name: Run pgpverify (non-blocking)
        id: pgpverify
        run: |
          cd "${{ needs.detect.outputs.java_location }}"
          set +e
          mvn -B -U \
            org.simplify4u.plugins:pgpverify-maven-plugin:1.19.1:check \
            --file pom.xml -s "$GITHUB_WORKSPACE/settings.xml" \
            2>&1 | tee "$GITHUB_WORKSPACE/pgpverify.log"
          code=${PIPESTATUS[0]}
          set -e
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          echo "pgpverify finished with exit code $code (non-blocking)."

      - name: Build pgpverify comment body
        run: |
          LOG="$GITHUB_WORKSPACE/pgpverify.log"
          OUT="$GITHUB_WORKSPACE/pgpverify-comment.md"
          CODE="${{ steps.pgpverify.outputs.exit_code }}"

          # Strip ANSI, then keep the signature-relevant WARN/ERROR lines the
          # plugin emits (unsigned / invalid / untrusted / missing-key entries).
          problems="$(sed 's/\x1b\[[0-9;]*m//g' "$LOG" 2>/dev/null \
            | grep -Ei 'pgp|signature|no key|not found|invalid|untrusted|not trusted|unsigned' \
            | grep -Ei 'warn|error|invalid|no signature|not found|untrusted|not trusted|unsigned|no key' \
            | head -100 || true)"

          if [ "$CODE" = "0" ]; then
            {
              echo "## 🔐 pgpverify — Maven Dependency Signature Check"
              echo ""
              echo "✅ All resolved dependency & plugin PGP signatures verified."
            } > "$OUT"
          else
            {
              echo "## 🔐 pgpverify — Maven Dependency Signature Check"
              echo ""
              echo "> ⚠️ **Warning — signature problems found (exit code \`$CODE\`).**"
              echo "> This check is **non-blocking**: it will **not** fail the PR, but the items below are supply-chain risks worth reviewing."
              echo ""
              if [ -n "$problems" ]; then
                echo "### 🚩 Warning signs"
                echo ""
                echo '```'
                printf '%s\n' "$problems"
                echo '```'
              else
                echo "_pgpverify reported a failure but no signature-specific lines were parsed — see the full log below._"
              fi
              echo ""
              echo "<details><summary>📋 Full pgpverify log (tail)</summary>"
              echo ""
              echo '```'
              sed 's/\x1b\[[0-9;]*m//g' "$LOG" | tail -300
              echo '```'
              echo ""
              echo "</details>"
            } > "$OUT"
          fi

      # Sticky comment on same-repo PRs only (a read-only token can't comment from
      # a cross-fork PR) — same guard as the Trivy filesystem job.
      - name: Post pgpverify findings comment
        if: github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.event.pull_request.base.repo.full_name
        uses: marocchino/sticky-pull-request-comment@0ea0beb66eb9baf113663a64ec522f60e49231c0  # v3.0.4
        with:
          header: pgpverify
          path: pgpverify-comment.md
